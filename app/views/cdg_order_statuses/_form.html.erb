<%= form_with(model: cdg_order_status, local: true) do |form| %>
  <% if cdg_order_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cdg_order_status.errors.count, "error") %> prohibited this cdg_order_status from being saved:</h2>

      <ul>
      <% cdg_order_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :is_active %>
    <%= form.text_field :is_active %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :sort_order %>
    <%= form.number_field :sort_order %>
  </div>

  <div class="field">
    <%= form.label :name_for_display %>
    <%= form.text_field :name_for_display %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
