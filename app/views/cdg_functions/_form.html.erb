<%= form_with(model: cdg_function, local: true) do |form| %>
  <% if cdg_function.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cdg_function.errors.count, "error") %> prohibited this cdg_function from being saved:</h2>

      <ul>
      <% cdg_function.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sort_order %>
    <%= form.number_field :sort_order %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
