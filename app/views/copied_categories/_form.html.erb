<%= form_with(model: copied_category, local: true) do |form| %>
  <% if copied_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(copied_category.errors.count, "error") %> prohibited this copied_category from being saved:</h2>

      <ul>
      <% copied_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :destination_pk_id %>
    <%= form.text_field :destination_pk_id %>
  </div>

  <div class="field">
    <%= form.label :copied_pk_id %>
    <%= form.text_field :copied_pk_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
