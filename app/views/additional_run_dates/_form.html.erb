<%= form_with(model: additional_run_date, local: true) do |form| %>
  <% if additional_run_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(additional_run_date.errors.count, "error") %> prohibited this additional_run_date from being saved:</h2>

      <ul>
      <% additional_run_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pk_id %>
    <%= form.text_field :pk_id %>
  </div>

  <div class="field">
    <%= form.label :run_date %>
    <%= form.datetime_select :run_date %>
  </div>

  <div class="field">
    <%= form.label :fk_order_id %>
    <%= form.text_field :fk_order_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
