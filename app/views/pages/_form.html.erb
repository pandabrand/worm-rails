<%= form_with(model: page, local: true) do |form| %>
  <% if page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :duration %>
    <%= form.number_field :duration %>
  </div>

  <div class="field">
    <%= form.label :is_master %>
    <%= form.text_field :is_master %>
  </div>

  <div class="field">
    <%= form.label :templateID %>
    <%= form.number_field :templateID %>
  </div>

  <div class="field">
    <%= form.label :preview %>
    <%= form.text_field :preview %>
  </div>

  <div class="field">
    <%= form.label :sortOrder %>
    <%= form.number_field :sortOrder %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
