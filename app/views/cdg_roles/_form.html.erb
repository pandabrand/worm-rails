<%= form_with(model: cdg_role, local: true) do |form| %>
  <% if cdg_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cdg_role.errors.count, "error") %> prohibited this cdg_role from being saved:</h2>

      <ul>
      <% cdg_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :is_active %>
    <%= form.text_field :is_active %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :sort_order %>
    <%= form.number_field :sort_order %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
