<%= form_with(model: lab_region, local: true) do |form| %>
  <% if lab_region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lab_region.errors.count, "error") %> prohibited this lab_region from being saved:</h2>

      <ul>
      <% lab_region.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :is_active %>
    <%= form.text_field :is_active %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :region_id %>
    <%= form.text_field :region_id %>
  </div>

  <div class="field">
    <%= form.label :sort_order %>
    <%= form.number_field :sort_order %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
