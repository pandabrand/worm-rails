<%= form_with(model: title3, local: true) do |form| %>
  <% if title3.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(title3.errors.count, "error") %> prohibited this title3 from being saved:</h2>

      <ul>
      <% title3.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :isActive %>
    <%= form.text_field :isActive %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :sort_order %>
    <%= form.number_field :sort_order %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
