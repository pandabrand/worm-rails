<%= form_with(model: user_upload_log, local: true) do |form| %>
  <% if user_upload_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_upload_log.errors.count, "error") %> prohibited this user_upload_log from being saved:</h2>

      <ul>
      <% user_upload_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fk_user_id %>
    <%= form.text_field :fk_user_id %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="field">
    <%= form.label :upload_date %>
    <%= form.datetime_select :upload_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
