<%= form_with(model: cdg_faq, local: true) do |form| %>
  <% if cdg_faq.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cdg_faq.errors.count, "error") %> prohibited this cdg_faq from being saved:</h2>

      <ul>
      <% cdg_faq.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :is_active %>
    <%= form.text_field :is_active %>
  </div>

  <div class="field">
    <%= form.label :answer %>
    <%= form.text_area :answer %>
  </div>

  <div class="field">
    <%= form.label :question %>
    <%= form.text_area :question %>
  </div>

  <div class="field">
    <%= form.label :sort_order %>
    <%= form.number_field :sort_order %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
