<%= form_with(model: order_status_tracker, local: true) do |form| %>
  <% if order_status_tracker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_status_tracker.errors.count, "error") %> prohibited this order_status_tracker from being saved:</h2>

      <ul>
      <% order_status_tracker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fk_order_status_id %>
    <%= form.text_field :fk_order_status_id %>
  </div>

  <div class="field">
    <%= form.label :date_submitted %>
    <%= form.datetime_select :date_submitted %>
  </div>

  <div class="field">
    <%= form.label :fk_order_id %>
    <%= form.text_field :fk_order_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
